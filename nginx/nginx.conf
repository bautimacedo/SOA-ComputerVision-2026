server {
    listen 80;
    server_name _;

    client_max_body_size 50M;

    location / {
        proxy_pass http://soa_app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 120s;
    }
}
